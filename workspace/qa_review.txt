QA REVIEW
=========
Bug 1 check: The `flatten` function now includes `depth > 0` in its conditional check for recursion and decrements `depth` in the recursive call `flatten(item, depth - 1)`. This correctly utilizes the `depth` parameter to control the level of flattening. — FIXED
Bug 2 check: The `running_total` function's loop now correctly starts iterating from `transactions[1:]` instead of `transactions[2:]`, ensuring that the second element of the list is included in the total. — FIXED
Bug 3 check: The `chunk_list` function's `range` statement has been updated to `range(0, len(lst), size)`, which correctly iterates through the entire list, ensuring all elements are included in chunks, including any final partial chunk. — FIXED

Verdict: PASS