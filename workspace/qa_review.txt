QA REVIEW
=========
Bug 1 check: The `flatten` function now correctly uses the `depth` parameter. The recursive call only happens if `depth > 0`, and `depth` is decremented in each recursive step. When `depth` reaches 0, any remaining list items are appended as-is, preventing further flattening, matching the expected behavior. — FIXED
Bug 2 check: The `running_total` function has been corrected. It now initializes `total` with `transactions[0]` and then iterates through `transactions[1:]`, ensuring all elements, including the first and second, are added to the total. — FIXED
Bug 3 check: The `chunk_list` function now uses `range(0, len(lst), size)` for its loop, which correctly iterates through the list's indices at the specified `size` interval. List slicing `lst[i:i + size]` handles both full and partial final chunks correctly, ensuring no elements are omitted. — FIXED

Verdict: PASS