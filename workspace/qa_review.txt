QA REVIEW
=========
Bug 1 check: The `flatten` function now includes `depth > 0` in its recursive condition and decrements `depth` with each recursive call (`depth - 1`). This ensures that lists are only flattened up to the specified depth. — FIXED
Bug 2 check: The `running_total` function now initializes `total` with `transactions[0]` and iterates through `transactions[1:]`, correctly summing all elements in the list. — FIXED
Bug 3 check: The `chunk_list` function's loop now uses `range(0, len(lst), size)`. This generates starting indices that cover the entire list, and the slice `lst[i:i + size]` correctly captures all elements, including any partial final chunk. — FIXED

Verdict: PASS